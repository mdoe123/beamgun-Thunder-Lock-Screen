<Window x:Class="BeamgunApp.ServerChanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server酱 SendKey"
        Height="220" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="SendKey：" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="SendKeyBox" Height="28" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="2" Text="在 sct.ftqq.com 申请的 SendKey，保存到 serverchan.json。" 
                   Foreground="Gray" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="#FF6B6B" MinHeight="18"
                   Margin="0,0,0,4" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="OkButton" Content="确定" Width="80" Height="30" Margin="0,0,8,0"
                    Click="OkButton_Click" IsDefault="True"/>
            <Button Content="取消" Width="80" Height="30" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
<Window x:Class="BeamgunApp.SetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置解锁密码"
        Height="240" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="新密码：" Margin="0,0,0,4"/>
        <PasswordBox Grid.Row="1" x:Name="NewPasswordBox" Height="28" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="2" Text="确认密码：" Margin="0,0,0,4"/>
        <PasswordBox Grid.Row="3" x:Name="ConfirmPasswordBox" Height="28" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="4" x:Name="ErrorText" Foreground="#FF6B6B" MinHeight="18"
                   Margin="0,0,0,4" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="OkButton" Content="确定" Width="80" Height="30" Margin="0,0,8,0"
                    Click="OkButton_Click" IsDefault="True"/>
            <Button Content="取消" Width="80" Height="30" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
